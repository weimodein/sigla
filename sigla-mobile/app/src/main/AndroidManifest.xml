<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    

    <application
        android:name=".SiglaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SIGLA"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sigla"
        android:usesCleartextTraffic="true"
        tools:replace="android:allowBackup,android:icon,android:label,android:theme"
>

        <!--
            Lets MediaPipe's GPU delegate dlopen the vendor OpenCL driver.

            Since Android 7 (and enforced hard on 12+), an app process can only load
            vendor native libraries that it declares here, even when the .so is in the
            platform's public.libraries allowlist. Without this, dlopen("libOpenCL.so")
            fails with:

                Failed to load OpenCL library with dlopen: dlopen failed:
                library "libvndksupport.so" not found. Trying ICD loader.

            MediaPipe does NOT surface that as an error — createFromOptions() still
            succeeds and the helper logs "ready [GPU]", but the graph silently runs on
            CPU. On this device that was ~109 ms per frame, capping the pipeline near
            5 fps, which is what made the preview and predictions feel laggy.

            android:required="false" so devices with no OpenCL driver still install and
            fall back to the CPU path as before.
        -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <!-- Vendor-specific OpenCL names used by some Arm/Qualcomm/MediaTek stacks. -->
        <uses-native-library android:name="libOpenCL-car.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />
        <uses-native-library android:name="libGLES_mali.so" android:required="false" />


        <!-- launchMode="singleTop" on the sidebar-reachable screens: re-tapping
             the current screen in the drawer reuses the existing instance
             instead of stacking (and rebuilding) a second copy of it. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".TranslationHistoryActivity" android:exported="false" android:launchMode="singleTop" android:screenOrientation="portrait" />
        <activity android:name=".WordBankActivity" android:exported="false" android:launchMode="singleTop" android:screenOrientation="portrait" />
        <activity android:name=".CategoryWordListActivity" android:exported="false" android:screenOrientation="portrait" />
        <activity android:name=".WordDetailActivity" android:exported="false" android:screenOrientation="portrait" />
        <activity android:name=".NotificationsActivity" android:exported="false" android:launchMode="singleTop" android:screenOrientation="portrait" />
        <activity android:name=".ProfileActivity" android:exported="false" android:launchMode="singleTop" android:screenOrientation="portrait" />
        <activity android:name=".SettingsActivity" android:exported="false" android:launchMode="singleTop" android:screenOrientation="portrait" />
        <activity android:name=".TermsActivity" android:exported="false" android:screenOrientation="portrait" />
        <activity android:name=".OnboardingActivity" android:exported="false" android:screenOrientation="portrait" />
        <activity android:name=".AuthActivity" android:exported="false" android:screenOrientation="portrait" />
        

    </application>
</manifest>