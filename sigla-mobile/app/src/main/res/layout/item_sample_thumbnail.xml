<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="3dp">

    <ImageView
        android:id="@+id/ivThumbnail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:background="#1A4A90E2"
        android:contentDescription="Sample thumbnail" />

    <!-- Motion badge overlay (play icon) -->
    <ImageView
        android:id="@+id/ivMotionBadge"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="4dp"
        android:src="@android:drawable/ic_media_play"
        android:imageTintList="@android:color/white"
        android:background="#CC001E3D"
        android:padding="4dp"
        android:visibility="gone" />

    <!-- Sample number badge -->
    <TextView
        android:id="@+id/tvSampleNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="4dp"
        android:textColor="#FFFFFF"
        android:textSize="9sp"
        android:textStyle="bold"
        android:background="#CC001E3D"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:paddingTop="1dp"
        android:paddingBottom="1dp" />

</FrameLayout>
