<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Focused state — bright blue border -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <stroke android:width="2dp" android:color="#4A90E2" />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- Filled (has text) — slightly lighter border -->
    <item android:state_enabled="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <stroke android:width="1.5dp" android:color="#B0C8F0" />
            <corners android:radius="10dp" />
        </shape>
    </item>

    <!-- Default -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#F8F9FF" />
            <stroke android:width="1dp" android:color="#D0D8EE" />
            <corners android:radius="10dp" />
        </shape>
    </item>

</selector>